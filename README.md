# Notely

Friendly note and list manager.

## Run locally

```bash
node server.js
```

Then open http://localhost:3000

Note: The custom server is required to set COOP/COEP headers for OPFS (Origin Private File System) support, which enables persistent SQLite storage in the browser.
