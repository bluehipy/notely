node_modules/
sqlite-wasm/
test-*.js
package-lock.json

# Machine-local native-messaging registration (absolute path + locally-loaded
# extension id) and its runtime log — neither is portable across machines
native-host/com.notely.bridge.json
native-host/chrome-launch.log

# Secrets / env files
.env
.env.*
*.pem
*.key
credentials.json

# OS cruft
.DS_Store
Thumbs.db
